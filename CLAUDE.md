# Code Editing
Check `hostname` if unsure which machine you're on.
- On sirius: this repo is local — use native Read/Write/Edit/Glob/Grep tools.
- On any other host: use Constellation File Access MCP with the `constellation-mcp` 
  label to reach this repo on sirius.