{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "paths",
  "description": "Opens the native directory picker for 'Browse...' when adding a path. Only the paths window does this.",
  "windows": ["paths"],
  "permissions": [
    "dialog:allow-open"
  ]
}
