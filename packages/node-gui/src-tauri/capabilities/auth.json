{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "auth",
  "description": "Opens the OAuth device-code verification URL in the system browser. Only the auth window does this.",
  "windows": ["auth"],
  "permissions": [
    "opener:default"
  ]
}
