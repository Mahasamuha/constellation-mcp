{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "common",
  "description": "Baseline permissions needed by every Constellation window (window close only; no dialog/opener/notification/autostart).",
  "windows": ["auth", "status", "paths", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
