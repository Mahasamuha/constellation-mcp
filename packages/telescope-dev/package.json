{
  "name": "@constellation/telescope-dev",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "description": "Local-only mock MCP Apps host for poking at packages/telescope's file browser without a real relay/agent/model in the loop.",
  "scripts": {
    "dev": "concurrently -n telescope,host -c blue,green \"npm run dev -w @constellation/telescope\" \"vite\"",
    "host": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.4"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "typescript": "^6.0.0",
    "vite": "^8.0.14"
  }
}
